//Switch 
switch(room)
{
	case rTesting:
		global.score.testLevel = roomScore
		show_debug_message("yeah i did it")
		show_debug_message(roomScore)
		show_debug_message(global.score.testLevel)
		break;
}
